#!/usr/bin/env ruby

require "csv"

music = `sp metadata`

csv = CSV.parse(music, { col_sep: "|" })
final_music = ""

csv.each do |row|
    final_music += row[1] if row[0] == "artist"
    final_music += " - #{row[1]}" if row[0] == "title"
end

print final_music
