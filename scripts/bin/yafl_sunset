#!/usr/bin/env ruby

if ARGV.length.positive?
  input = ARGV[0]
  if input.end_with? "on"
    `wlsunset`
  elsif input.end_with? "off"
    `pkill wlsunset`
  end
else
  puts "Turn sunset on\nTurn sunset off"
end
