#!/usr/bin/env bash

if [ -z $@ ]; then
    ROFI_RETV=0 rofimoji | tail -n +4
else
    echo $1 >> /tmp/hi.log
    ROFI_RETV=0 rofimoji > /dev/null
    ROFI_RETV=1 rofimoji --skin-tone=neutral --action=type -- "$1"
fi
