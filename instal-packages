sudo pacman -Syu tar curl wget emacs dunst jq meld openssl-1.0 pulseaudio redshift strace scrot tree ttf-dejavu unp unzip xclip

wget https://aur.archlinux.org/cgit/aur.git/snapshot/yay.tar.gz
tar -xzvf yay.tar.gz
pushd yay
makepkg -si
popd
rm -rf yay

yay albert
