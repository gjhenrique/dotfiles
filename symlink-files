#/bin/bash

stow --adopt files -t ~

if [ "$1" = "home" ]; then
     stow scripts -t ~
else
    sudo stow scripts -t /opt
fi

stow --adopt conky -t ~


# Instalar spacemacs OK
# Instalar oh-my-zshrc OK
# /home/guilherme/.vimperatorrc/colors - repositorio git OK

# Script para criar playlist do mpd

# Instalar rbenv
# Intalar pyenv
# Instalar nvm

# Instalar conky, hsetroot
# Atualizar packages
# mv ~/.zshrc.pre*
# Patch do vimperator colors
# Tema do GTK

# No script de nova máquina, colocar a mudanca de shell para zsh. Copiar do oh-my-zsh
